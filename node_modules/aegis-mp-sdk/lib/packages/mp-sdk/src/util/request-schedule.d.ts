import { SendOption, SendSuccess, SendFail } from "../../../core/src";
declare type Task = {
    options: SendOption;
    success?: SendSuccess;
    fail?: SendFail;
};
export declare class RequestSchedule {
    private taskQueue;
    private count;
    private sendRequest;
    private maxCount;
    constructor(sendRequest: Function, maxCount?: number | undefined);
    addTask: (task: Task) => void;
    fireTask: () => void;
    complete: () => void;
}
export {};
